import React from "react";
import SettingsTab from "../components/settingsComponents/SettingsTab";

const Settings: React.FC = () => {
    return (
        <div className="mt-8 select-none">
            <SettingsTab />
        </div>
    );
};

export default Settings;